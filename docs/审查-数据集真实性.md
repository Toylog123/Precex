# PreCex 项目审查：数据集真实性（跨周期定义 / 弱 tb 消毒 / 缺陷深度）
> 作者：Codex 审查｜日期：2026-08-05｜性质：项目级审查记录（用户质疑"真的跨周期修复吗"触发）
> 结论先行：**当前 34 样本数据集的"L3（弱 tb 通过 + 形式验证失败）"与"跨周期缺陷"叙事存在系统性失真**，需要用户决策是否重构。

## 一、审查动机
用户质疑：修复率 100% 是否合理？样本真的是"跨周期缺陷"吗？

## 二、证据 1：弱 tb 通过是"消毒"构造的，非自然成立
- scripts/bug_injector.py L66-78 注释：**"弱 tb 要求放过缺陷"**；WEAK_TB_STRIP 按 error_type 定义剥离关键字表（full/empty/count/state/done/txd/rx_valid 等）。
- _weaken_tb() L81-113：注入时删除命中关键字的 $fatal 检查行（含两行式 if 剥离）。
- L516 注释：**"弱 tb 仿真：必须放过 buggy（exit 0 且无 FAIL/$fatal 且含 $finish）"**——放过是硬校验。
- 实测：**32/34 样本（94%）带 sanitize 标记**；s30/s33 未消毒（弱 tb 完整保留 $fatal 检查仍放过 buggy）——**这两个是仅有的天然 L3 样本**，证明天然 L3 真实存在但占比极低（2/34 = 6%）。

## 三、证据 2：跨周期深度明显注水（34 样本 fail_step 分布）
| 反例长度 | 数量 | 占比 | 样本 |
| --- | --- | --- | --- |
| ≤4 拍 | 22 | 65% | s12 s22 s10 s25 s26 s30 s09 s11 s13 s15 s18 s20 s21 s29 s31 s06 s16 s19 s27 s28 s33 s34 |
| 5-12 拍 | 10 | 29% | s08 s17 s04 s07 s14 s05 s23 s32 s36 s37 |
| ≥20 拍 | 2 | 6% | s24(35) s35(39) |
- 中位数 4 拍、均值 6.7 拍。65% 样本 BMC 在 4 拍内抓到反例，弱 tb 若未被消毒，多数只需打几十拍即触发。

## 四、证据 3：缺陷语义深度分层（人工归类）
| 深度 | 数量 | 占比 | 说明 |
| --- | --- | --- | --- |
| L1 单拍逻辑/复位 | 15 | 44% | 比较符/门控/复位值错误，无需历史状态 |
| L2 跨一拍状态 | 16 | 47% | 状态跳转/指针守恒/握手保持，需上一拍信号 |
| L3 真跨周期 | 3 | 9% | s35/s36/s37 UART 帧时序，需完整位周期 |

## 五、证据 4：弱 tb 参数不一致（s35）
- tb_weak.sv 用 CLK_FREQ=50MHz/BAUD=115200（DIV=434）做采样时序，但 uut 实例化 CLK_FREQ=400/BAUD=100（DIV=4）。采样点与设计位周期不匹配，tb 有效性存疑。

## 六、对论文叙事的影响
1. **"跨周期行为缺陷是综合前最难定位修复的缺陷类型"** 缺乏数据支撑：44% 单拍 + 65% 反例≤4拍。
2. **"弱测试平台通过而形式验证失败"** 的 L3 定义被消毒机制掏空：弱 tb 通过是被构造的。
3. 修复率 100% 在浅层缺陷上可预期，不能证明 LLM 解决了"深时序修复"。

## 七、仍然成立的部分（不可抹杀）
- B 结构化证据定位 61.8% vs A 47.1%（p=0.0035）的受控消融，在"证据组织影响定位"层面可信。
- 判据翻案（prove→BMC，golden-first 规则）是真实的方法论发现。
- 验证充分性闭环（变异 88.5%/T2 审计 408/408）作为工程资产有效。
- s24(35拍)/s35(39拍)/s36/s37(176深度) 证明管线的形式验证深度能力真实存在。

## 八、建议方向（需用户决策）
- **A. 降级叙事**：论文定位改为"反例驱动的 RTL 缺陷定位修复 + 验证充分性闭环"，明确数据集为注入+消毒的受控基准（类似 mutation testing 语义），不宣称"跨周期最难"。
- **B. 重构数据集**：大幅增加 ≥20 拍真深时序缺陷样本（≥50%），重新跑实验与论文。
- **C. 组合**：先按 A 保住现有成果，把 B 列为未来工作/扩展实验。

## 九、待办（审查后续）
- [ ] 用户决策方向（A/B/C）
- [x] 核实 s30/s33：**天然 L3**（未消毒，s30 保留 3 处 $fatal、s33 保留 12 处，弱 tb 仍通过 buggy）
- [ ] 全量核查 34 个 tb_weak 的参数一致性
