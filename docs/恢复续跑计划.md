# 环境恢复后续跑计划（2026-08-06 11:40 阻塞快照）

> 状态：python/WSL/git写 均因系统登录会话异常暂时不可用（数据完好，未丢）。
> 本文档记录恢复后需执行的步骤，确保无缝续跑。

## 当前数据状态（已核验）

| 数据集 | 结果数 | 成功 | 备注 |
| --- | --- | --- | --- |
| 主实验 C（s04-s37） | 102 文件 | 73 | 28 个旧污染需清理重跑 + s14/C/1 真实 FAIL |
| L2 C（24 样本） | 63/72 写盘 | 53 | 9 个缺失（2 个跑中被打断）+ 12 个异常 |
| deep C（s38-s42） | 15 + retry 8 | 7+5 | s42 顽固 API 超时需低峰重跑 |
| C 摘要 | 63/63 DS 版 | — | MM 版已备份 semantics_mm.json |

## 恢复后步骤（按序）

### 1. 环境自检
```powershell
python -c "print('ok')"          # 必须成功
wsl -e bash -c "echo wsl-ok"      # 必须成功
git add -A --dry-run              # git 写权限恢复
```

### 2. 主实验 C 缺失任务重跑（28 个旧污染 + s14 重跑）
- 分片脚本已备好：experiments/runs/.c_ds_sh/exp_c_fix_*.sh（8 片，29 任务，retries=3）
- 启动：wsl -e bash -c "for i in 0 1 2 3 4 5 6 7; do bash experiments/runs/.c_ds_sh/exp_c_fix_$i.sh; done"
- 注意：这些分片用 --out exp_c_fix_*.json（新文件），不会复用旧 partial

### 3. L2 补缺
- 缺失：对比 exp_cl2_ds_*.json 与 24×3=72，找出未写盘的 9 个任务重跑
- 异常 12 个（API 超时 + uart_tx 真实 FAIL/INCONCLUSIVE）重跑

### 4. deep 顽固项（s42 等）
- s42/C/seed0/1/2 反复 API 超时（深时序 75 拍大 token）
- 低峰时段（深夜）降并发（2-3 进程）重跑，或提高 LLMClient timeout=240

### 5. MM 单裁判可解释性评分
```powershell
python scripts/llm_interpretability.py --providers minimax --samples s04-s37 --n 10 --out experiments/runs/llm_scores_mm
```
- 20 次 MM 调用（10 样本 × C/D），约 $0.1
- 输出 MM 绝对评分（无 ICC，单 provider）

### 6. 数据合并 + 审计
- 合并 exp_c_fix_* + exp_c_ds_clean.json → 新 leakfix_merged_clean 版
- 更新 clean_stats.json
- 更新 audit_paper_numbers.py（C 结果、ICC → MM 绝对评分）

### 7. 论文改写（可解释性小节）
- "双模型 ICC" → "MM 独立裁判绝对评分"
- 披露：C 摘要 DS 生成、修复 DS 执行、可解释性 MM 评分——无角色重叠

### 8. 提交推送
- git add + commit + push（网络已恢复，用 -c http.sslVerify=false 若证书问题）

## 关键文件
- 主实验分片：scripts/run_experiments.py（--tasks 断点续跑）
- 摘要重生成：scripts/regen_text_summary.py（含 skip 逻辑）
- 改造文档：docs/MM裁判改造.md
- 待重跑任务清单：见本文档第 2-4 节
