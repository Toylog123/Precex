# PreCex 后续推进方案计划 v4

> 更新：2026-08-07 17:20 | 基于 v3 执行结果修订，v3 已完成条目在此标注状态；执行主线、门禁与 v3 一致，新增两条工程纪律。

## 0. 现状快照（相对 v3 的新事实）

| 条目 | 状态 |
|---|---|
| 阶段 0 失败档案（40 miss 全归类） | 完成 |
| 阶段 1a apply_diff 修复 + s10 回归 | 完成；v2 全量重扫后台运行中 |
| 阶段 2a eq_sem 12 条 | 完成：loc dev=0，11/12 修复 PASS |
| 阶段 2a off8 16 条 | 完成：loc dev=0，16/16 修复 PASS |
| 阶段 2a near1_other 9 目标 | 完成（17:14）：修复 PASS 9/9，exact 7/9，±1 8/9 |
| 阶段 2a-4 s15 2×2（12 次） | 完成：exact 0/12，±2 行 9/12，±6 行 12/12，修复 PASS 11/12 |
| 阶段 3 k-归纳扫描 | 完成：73.5% < 80% 门禁，判据升级延后，决策已落盘 |
| deep_bd 12 目标 | 事故：第二次静默退出，JSON 未落盘，需重跑 |

核心结论：
- C exact 外推 = (62 + 12 + 16 + 7) / 102 = 97/102 ≈ 95.1%（±1 口径 98/102 ≈ 96.1%），远超 2a 门禁 75%。
- 已处理 37 个 miss 目标中 36 个修复 PASS（eq_sem 仅 1 条定位 dev=0 但 diff 应用失败）→ 原始失败的本质是证据/判据卫生问题，不是模型能力。
- s15 2×2 揭示删除型缺陷的判据问题：被删语句在 buggy 中不存在，exact 行号判据系统性过严；reason 文本 12/12 正确指认机制，瓶颈在证据形态与判据口径。

## 1. 主线与两条新纪律

- A（API 线）：1b structural formal → 2b/2c 握手证据迭代 → 固化
- B（本地线）：1a depth scan 收尾 → 判据卫生全量回归
- 纪律 1：后台任务 stdout 必须落盘、每样本增量落盘、结束钩子校验 JSON 存在、异常捕获（deep_bd 两次事故教训）。
- 纪律 2：负结果与事故当日写入 docs/进度.md，不隔夜。

## 2. T0 收尾（今天，约 2-3 小时）

1. depth_scan_full_v2 收尾：确认 s25/s27/s28/s33/s34 深深度判定；TIMEOUT 分类（2x PASS 且 4x/8x 超时 = 资源问题而非深度敏感）。
2. deep_bd 重跑（12 目标，约 0.07 美元）：加固脚本（增量落盘 + 异常捕获 + 结束校验），stdout 重定向到 hygiene_deep_bd.log。
3. near1_other 分析：exact / ±1 / ±2 / repair 四口径统计，更新 failure_catalog.md 勾选状态。
4. 更新 docs/证据卫生根因分析.md 的 C 外推表（含 s15 容差口径）。
5. Git checkpoint 提交推送（Ground Truth 修复 + 新脚本 + 新文档 + hygiene JSON）。
6. 更新 docs/进度.md（本计划 + 本轮结果）。

## 3. 阶段 1b：StructuralRepairer 完整 formal 验证（API 线，约 1 天）

目标：证明结构性修复模式在 hardest 状态迁移样本上的增益可复现，且不损伤 BMC 通过率。

1. 6 样本（s07/s08/s09/s18/s36/s15）× {plain, structural} × 3 seeds = 36 次调用，走完整 compile → sim → BMC 管线。
2. 输出 experiments/runs/exp_structural_formal.json：loc（exact / ±1 / ±2）、diff 形态（最小 vs 结构性重写）、verdict、BMC depth。
3. 双口径分析：定位率 + 修复正确性 + 非最小 diff 占比。

门禁：
- structural 不损伤 BMC 通过率（保持 100%）；
- structural 定位增益 ≥ 1 样本，或 diff 形态差异有明确证据；
- 若无增益：如实报告为探索性负结果，采用定位/修复解耦（structural 仅定位，修复仍最小 diff）。

## 4. 阶段 2b/2c：握手/删除型缺陷证据形态迭代（核心难点，1-1.5 天）

### 4.0 判据口径定稿（0.5 天，先做）
- 三口径并存：exact / ±2 / ±6 + 修复正确性；主指标 = 修复 PASS 且 loc_dev ≤ 2。
- 论文同时报告 exact 与 ±2，避免删除型缺陷被低估。
- 叙事决策：主实验保留原始口径（A/B/C/D 对比），卫生口径作为独立对照实验，防止选手兼裁判质疑。

### 4.1 证据注入（1 天）
- 把 s15 2×2 的 handshake 变体固化进 EvidenceEngine：key_signal_diffs（应翻转未翻转信号）+ 归属 RTL 行（trace 反向定位，非断言行）。
- 扩展到全部握手/状态迁移样本（s07/s08/s09/s18/s36/s15 + uart_rx 深时序）。
- 兜底：golden vs buggy 失败窗口信号差异表（trace 层已实现，补 RTL 行映射）。

### 4.2 验证与门禁
- 握手类 loc±2 ≥ 75%（当前 s15 ±2 = 9/12 = 75%，边缘达标）；
- s15 repair ≥ 2/3（当前 11/12）；
- 目标：handshake 变体下 s15 loc±2 从 9/12 提升到 11-12/12。

## 5. 判据卫生固化 + 全量回归（0.5 天）

- 将 eq_sem / off8 / 绝对行号修复从 run_hygiene_ablation.py 固化到 evidence_engine.py + prompt_templates.py 主路径。
- 全量 C hygiene 重跑（102 条，约 0.5 美元）→ 新 C exact 终值。
- 更新 clean_stats.json + 论文表 tab:main（按 4.0 叙事决策处理主实验/消融双口径）。
- 门禁：卫生口径 C exact ≥ 90%。

## 6. 阶段 4：论文整合（1 天）

1. 叙事升级：核心贡献 = 数据卫生（ground-truth 行号重评，0 下翻转）+ 判据卫生（failed_line 语义纠偏、行号基准统一、删除型容差、BMC 收敛性分析）。
2. sec_evidence_hygiene.tex 补数字：28/28 + near1 9/9 + s15 2×2。
3. k-归纳负结果（73.5%）写入 §VI（决策文档已落盘）。
4. audit 三件套（numbers / refs / ground_truth）+ xelatex 编译 + 图重生成。
5. 提交推送 GitHub。

## 7. 执行节奏（约 3-4 天）

| 天 | 工作 |
|---|---|
| D1（今天） | T0 收尾：depth scan 判定、deep_bd 重跑、near1 分析、checkpoint 提交推送 |
| D2 | 1b structural formal 36 调用 + 判据口径定稿 |
| D3 | 2b/2c 握手证据迭代 + 固化主管线 |
| D4 | 全量 C 回归 + 论文整合 + 审计 + 推送 |

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| deep_bd 再次崩溃 | 增量落盘 + 结束校验 + stdout 重定向 + 单样本续跑 |
| structural 无增益 | 定位/修复解耦，如实报告，方向转向证据形态 |
| 握手归属行定位失败 | golden-vs-buggy 差异表兜底，主指标切容差口径 |
| API 不稳定 | 重试队列 + 低峰并发 4 + 断点续跑（token_ledger 保障） |
| 论文口径争议 | 主实验/消融双口径并存，audit 脚本强制校验 |

## 9. 成功标准（阶段 4 出口）

- C 定位：卫生口径 exact ≥ 90%，修复 PASS ≥ 95%；
- 握手类：loc±2 ≥ 75%，s15 repair ≥ 2/3；
- 验证：BMC 通过率 100% + k-归纳 73.5% 负结果如实报告；
- 论文：电子学报模板编译通过、audit 三件套 PASS、中英摘要一致；
- 交付：docs/进度.md 更新、GitHub 提交推送。
