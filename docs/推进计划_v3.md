# PreCex 后续推进方案计划 v3

> 日期：2026-08-07 ｜ 基线：Ground Truth 行号判据修复完成 + failure_catalog.md 落盘 + 论文审计全 PASS（未提交）

## 0. 现状锚点

- 行号判据修复完成：A 87.3% / B 75.5% / C 60.8% / D 73.5% / deep 80.0%，零下降翻转，论文已全量更新并编译通过。
- 失败档案（docs/failure_catalog.md）已把 C 设置 40 个 miss 全部归类：
  - eq_sem（12）：LLM 直接采信断言行（failed_line 语义错位）；
  - off8（16）：loc = 真值 - 8，行号基准偏移，根因未完全解释；
  - near1（6）：dev <= 1 近失；
  - other（6）：真实 miss；
  - 握手 s15 全设置 12/12 未精确命中（但 8/12 已 ±1 行）；
  - deep B/D dev≈8（12 条），疑与 off8 同源。
- 阶段 1a 卡点：scripts/bmc_depth_scan.py 的 apply_diff 只取 repairs[0]、不做上下文校验，导致 s10/s17/s22 补丁应用后语法错误（重复行/行错位）。

## 1. 主线总览：两条并行轨 + 一个合并点

| 轨道 | 内容 | 依赖 |
|---|---|---|
| A（API 依赖） | 1b structural formal → 2a 证据卫生重跑 → 2b/2c 握手证据迭代 | DeepSeek API |
| B（本地计算） | 1a 深度扫描修复重扫 → 3 k-归纳可行性扫描 | iverilog/yosys/sby |
| 合并点 | 2b/2c 结果 + 3 决策 → 阶段 4 论文整合 | — |

两轨完全独立，可同时启动。

## 2. 阶段 1a：深度扫描收尾（轨道 B，约 0.5 天）

任务：
1. 修 apply_diff：上下文行校验 + 连续 hunk 按序应用 + 应用后语法自检（iverilog -tnull / yosys read）；单个 repair 候选失败自动换下一个 (setting,seed)，记录实际使用的 diff 来源。
2. 回归：先单跑 s10/s17/s22 确认语法错误消失。
3. 全量重扫 34 样本 × {1x, 2x, 4x, 8x, 10x}。
4. s25/s27/s28 TIMEOUT 提高 budget 重扫，区分「资源超时」与「深度敏感」（2x PASS 而 10x 超时即为资源问题）。

门禁：34 样本全部有明确 PASS/FAIL/TIMEOUT 判定，0 个未分类；产出 depth_scan_full_v2.log/json。

## 3. 阶段 1b：StructuralRepairer formal 验证（轨道 A）

任务：
1. 6 个 hardest 状态迁移样本（s07/s08/s09/s18/s36/s15）× {plain, structural} × 3 seeds，走完整 compile → sim → BMC 管线。
2. 产出 experiments/runs/exp_structural_formal.json（loc + verdict + diff）。
3. 对比两模式：定位率（exact 与 ±1 双口径）、修复形态（structural 是否产生非最小 diff）、BMC 通过率。

门禁：structural 不损伤 BMC 通过率（保持 100%）。若损坏，将定位与修复解耦（structural 只用于定位）。

## 4. 阶段 2：定位率提升（核心，按失败档案根因逐条打）

### 2a-1 证据卫生：failed_line 语义纠偏（eq_sem 12 条）
- 改 agents/evidence_engine/evidence_engine.py：failed_line 不再直接写 cex.log 断言行，改为「缺陷相关行候选」（由轨迹分析给出失败窗口内翻转/静默信号所在 RTL 行），或去掉行号、只给代码内容。
- 目标样本：s08/s11/s17/s18/s22/s32 等 12 条。

### 2a-2 证据卫生：-8 偏移根因（off8 16 条）
- 先做根因实验：导出 s05/s06/s20/s21 等样本完整证据文本，逐行对照 buggy.v 行号体系，确认偏移来源（golden 行号？yosys 内部信号行号？证据模板插入行）。
- 修生成器后重跑 16 条。

### 2a-3 deep B/D dev≈8（12 条）
- 与 off8 同源则随 2a-2 一起修；独立则单独根因并落盘。

### 2a-4 握手 s15 2×2 分解实验
- 因素1 证据：当前 vs +「应翻转未翻转」静默信号轨迹；因素2 prompt：直接修复 vs 分步推理（先列状态变量 → 推断期望转移 → 再给 diff）。
- 4 格 × 3 seeds = 12 次调用（廉价），判定瓶颈是「证据缺信息」还是「模型推理」，直接决定 2b/2c 方向。

门禁（2a 整体）：C exact >= 75%（现 60.8%）；off8 聚类清零；deep B/D dev≈8 清零或根因落盘。

### 2b/2c：握手证据形态迭代（按 2a-4 结果）
- 证据因素主导 → CexTracer 增加静默信号提取：同激励下 cex 轨迹 vs golden 轨迹在失败窗口的信号差异，找出「应翻转未翻转」信号及其 RTL 行，注入证据。
- prompt 因素主导 → 结构化修复协议模板（状态变量枚举 → 转移条件核对 → 最小 diff）。
- 扩样：s15 + 其余握手/状态迁移样本（s07/s08/s09/s18/s36）。

门禁：握手类 loc exact 均值 >= 75%；s15 exact >= 2/3（当前 0/12 exact，但 8/12 已 ±1 行，修复后应显著）。

## 5. 阶段 3：验证完备性升级（k-归纳可行性，轨道 B 并行）

任务：
1. 34 个 golden 设计跑 k-归纳（sby prove 模式或 yosys bmc -g），统计收敛率。
2. 收敛率 >= 80% → 验证判据升级为「BMC 深度 + k-归纳证明」双轨，修复成功定义同步升级。
3. < 80% → 保留 BMC 有限深度口径，输出诚实局限 + CEGAR-lite（golden 反例驱动的归纳不变式）作为未来工作。

门禁：决策记录落盘（docs/验证完备性决策.md），不悬置。

## 6. 阶段 4：论文整合与交付

1. 按新结果更新 paper/manuscript/precex_paper.tex（中文、电子学报模板）+ abstract_en.tex。
2. 新增/改写内容：证据卫生（failed_line 纠偏 + 行号基准）作为「判据卫生」的姊妹贡献；握手证据形态改进结果；验证完备性升级或诚实局限。
3. 重跑审计三件套（audit_paper_refs / audit_paper_numbers / audit_ground_truth）+ xelatex 编译 + 图重生成（make_paper_figs.py）。
4. 更新 docs/进度.md、docs/cover_letter.md、投稿包；提交并推送 GitHub（网络已恢复）。

## 7. 并行策略与 API 预算

- 立即并行启动：1a（本地）+ 1b（API）+ 2a-1/2a-2 根因分析（本地读证据）+ 3（本地扫描），互不阻塞。
- API 预算：2a 重跑约 30-60 次 + 2b/2c 约 30-60 次，全走 DeepSeek v4-flash，成本低；失败进重试队列，低峰并发 4。
- 每阶段完成即更新 docs/进度.md，并在 failure_catalog.md 勾选门禁状态。
- 负结果规则：任何负结果必须拆到根因（代码 / 证据 / 模型三分），禁止以「未知」关闭。

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 2a-2 的 -8 偏移根因查不出 | 兜底：证据中统一去掉行号、只保留代码片段（跨设置一致，反而提升一致性） |
| k-归纳收敛率低 | 不改判据，论文如实报告 + 保留为未来工作 |
| structural formal 破坏 100% BMC 通过率 | 定位与修复模式解耦：structural 只用于定位，修复仍最小 diff |
| API 不稳定 | 重试队列 + 低峰并发 4 + 断点续跑 |

## 9. 建议执行节奏（约 5 个工作日）

| 天 | 工作 |
|---|---|
| D1 | 提交现有 Ground Truth 修复批次（checkpoint）；1a 修脚本 + 回归；1b 启动 |
| D2 | 1a 全量重扫 + 3 k-归纳扫描（并行）；2a-1/2a-2 根因分析 |
| D3 | 2a 证据修复重跑 + 2a-4 s15 分解实验 |
| D4 | 2b/2c 握手迭代 + 门禁评估 |
| D5 | 阶段 3 决策 + 阶段 4 论文整合 + 审计三件套 + 推送 |

## 10. 成功标准（阶段 4 出口）

- 定位：C exact >= 75%（基线 60.8%），握手类 exact >= 75%，s15 >= 2/3；
- 验证：k-归纳收敛率 >= 80% 则判据升级，否则诚实局限落盘；
- 论文：电子学报模板编译通过，审计三件套全 PASS，中英文摘要一致；
- 交付：docs/进度.md 更新、提交推送 GitHub。
